# Without this, some servers return the manifest as octet-stream and the
# browser ignores it, which loses the PWA's name and icon.
/manifest.webmanifest
  Content-Type: application/manifest+json

# JS and CSS carry a hash in the filename, so they're immutable.
/assets/*
  Cache-Control: public, max-age=31536000, immutable
